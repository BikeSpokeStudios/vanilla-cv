


// Create function 
function getTotalCampgroundSize(campgrounds) {

    //Create a total variable with a value of 0

    // Create a for loop that will run once for every element in the campgrounds array

        //Inside the for loop, access the value in the partySize key of the object you're currently iterating over

        //Calculate (+) the sum of PartySize and total

        //Store the sum of that calculation in the 'total' variable

    // Return that value stored in the total variable

}